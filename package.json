{
  "name": "lol-roulette-rng-netlify-ready",
  "version": "6.0.0",
  "description": "Roulette RNG LoL full random, prête pour Netlify.",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.js",
    "dev": "npm run build && npx serve dist",
    "clean": "node -e \"require('fs').rmSync('dist', { recursive: true, force: true })\""
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "serve": "^14.2.4"
  }
}
